Faire en sorte que le transpiler
ajoute le contenu des headers inclus dans c.cpp
au code.

Faire en sorte que le showmain refonctionne
suite aux ifdef de l'exo3.
Faire un affichage style :
'''
c.cpp::client:
...

c.cpp::serveur:
...
'''

Faire en sorte que la transformation des fonctions
void en bool fonctionne dans le transpiler

Essayer de faire fonctionner les programmes sans avoir à les build
(avec cplusplus en passant l'option -D qui est transmise à g++)


____________________________________________________________________________

Faire en sorte que le transpiler, si il y a des includes de fichiers
dans le même répertoire que le source, on les ajoute au source (flux)
avant de transpiler.

Trouver un meilleur moyen de randomizer les noms des fichiers out dans
cplusplus.

Changer l'exemple du code pour quelque chose de plus simple, avec 
moins de code.
Moins de traitement côté serveur (genre une ligne par cas).
Par exemple afficher quelque chose ?

--------------------------------------------------------------------
Être capable de faire :

#BUILD BINARIES
g++ -x c++ <(./c.cpp) -D client -o client
g++ -x c++ <(./c.cpp) -D serveur -o serveur

# RUN (COMMUNICATION WITH A PIPE)
prog='eval cplusplus <(./c.cpp) -D'; $prog client | $prog serveur -

# RUN (COMMUNICATION THROUGH A NETWORK)
## serveur
nc -l localhost 55555 | cplusplus <(./c.cpp) -D serveur
##client
cplusplus <(./c.cpp) -D client | nc localhost 55555 -q1
--------------------------------------------------------------------